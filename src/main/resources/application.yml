spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false
  output:
    ansi:
      enabled: always

base-url: /api

server:
  address: 0.0.0.0
  port: ${PORT:8080}

#rsa:
#  private-key: classpath:certs/private.pem
#  public-key: classpath:certs/public.pem